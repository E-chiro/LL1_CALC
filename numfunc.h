#ifndef NUMFUNC
#define NUMFUNC
#include <cmath>
#include <limits>
#include <vector>
using namespace std;


double truncate(double);
double divide(double, double, bool=true);
double modulo(double, double, bool=true);
bool is_prime(double n);


vector<long> divs(long);
long gcd(long , long );
long lcm(long , long );



#endif // NUMFUNC

